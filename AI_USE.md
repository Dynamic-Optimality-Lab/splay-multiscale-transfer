# AI_USE.md — SPLAY-AM-MST-v0.3 AI assistance disclosure (INV-065)

## Assistant identity

Implementation performed with the OpenCode coding agent (model family noted in
workspace as Muse Spark) across WP-0…WP-6 turns, under standing user
instructions (stale-clearance rule, commit+push without prompting, Path/WorkPlan
depth rule). No statistical/ML models were trained as scientific objects at any
point (the experiment's "models" are finite-alphabet transfer calculi
instantiated by exact solvers).

## What the assistant did

Scaffolded the repo; implemented the Splay/Pair-Access cores, rotation
tracers, L6 translation modules, provenance/ledger/grammar machinery, solver
encodings, adversary engines, freeze/evaluation/clean-room/large-n/seal
machinery, all runners, all test suites, proof documents (author-claim
PROVED at most), and all reports including this file. Every turn ended with
VERIFY → COMMIT → PUSH and a Path.md adherence record with gaps versioned.

## What the assistant did not do (human-owned acts)

- Theorem REVIEWED verdicts: the ten ACCEPT records (`math/reviews/
  MST0-01…16` except 09) were returned by the human reviewer on 2026-09-23 and
  recorded schema-validated; computational reproduction was never presented as
  review.
- Fresh-reveal authorization: WP-5 entry was explicitly approved by the user
  after the read-only reconstruction report; the one-way reveal ran only then.
- Pending reviews (MST0-13/23/24/26 packages) await human verdicts; none is
  pre-counted and FINAL_RESULT consumes none of them.
- Parent seals, literature bytes, and firewall states were verified from
  artifacts, never generated.

## Claim discipline

No finite result was upgraded to a theorem by the assistant at any point; the
seal's terminal claim (`TRANSFER_CALCULUS_SURVIVES_FINITE_TESTS`) and all
NOT_APPLICABLE/BLOCKED/UNPROVED records reflect machine-verifiable artifact
state. Failed runs, killed candidates, and fixed bugs are retained in the
record (Path.md, atlases, reveal files), never deleted.
