# Theorem MST0-21 — unbounded critical-cycle motif implies negative result only after closed-form family

**Status:** NOT_APPLICABLE — preserved justification in
`math/reviews/MST0-21.not_applicable.json`. The negative family was never
activated (N1–N5 gate: N3 actual-ratio growth fails 11/11; all motif inflations
yield CONSTANT actual ratios). No closed-form `(T_k, X_k, Y_k)` family is
pursued or claimed. This record must never be consumed as a negative theorem.

## Justification

- WP-4 triage: 11 decisive-counterexample motifs inflated ×1/2/4/8; actual
  ratios constant (= cycle ratio, e.g. 16/16 → 128/128); N3 fails everywhere →
  NEGATIVE_FAMILY_NOT_ACTIVATED (`artifacts/v03/adversarial/triage.json`).
- WP-5 fresh kills are representation obstructions (unpaid residuals at frozen
  C), not conjecture obstructions: no growing actual Splay ratio was observed
  (§32.10; INV-045/046 honored).
- Hence no reverse-bridge audit is owed and `DYNAMIC_OPTIMALITY_DISPROVED` is
  not claimed and not approached.

## What would activate this obligation

An explicit construction with growing actual ratio and diagonal-rooted
legality, proved in closed form with symbolic `Splay(X_k) ≤ f(k)`,
`Splay(Y_k) ≥ g(k)`, `g/f → ∞`, plus independent replay. Nothing in the
record meets this bar.
