# Theorem MST0-15 — local transfer calculus integrability (status record)

**Status:** UNPROVED — no arbitrary-n integrability proof exists. The finite
ledgers are append-ordered and deterministic (MST0-10 REVIEWED), which is
necessary but not sufficient for decomposition-independent global energy
accounting. No review verdict is requested. This record must never be consumed
as a theorem (first consumer WP-6 requires REVIEWED).

## Why no proof is claimed

Integrability (local T7/T5/T6 rules define a legitimate global energy/flow
accounting independent of arbitrary decomposition choices) requires the KEEP
repayment theorem (MST0-14, UNPROVED) plus the universal reference-locality
theorem (MST0-08 universal, UNPROVED with explicit gap). Neither prerequisite
holds, so integrability is asserted for no candidate at any n beyond the
executed finite episodes.

## Finite facts (not a proof)

Every executed episode (development, H3T 70k × 3 candidates, large-n) ran the
deterministic update in canonical order with exact `Fraction` arithmetic;
ledger before/after states are preserved for first-violation episodes in
`artifacts/v03/holdouts/h3t_reveal.json`. Order-dependence beyond the canonical
order is untested and unclaimed.
