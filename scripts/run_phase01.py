"""Spec PHASE 01 runner (STUB): owned by WP-1, entry gate FOUNDATION_FROZEN + WP-1 pre-consumption subgate (MST0-01 REVIEWED).

This stub exists so every spec phase has an executable entry point.
It refuses to run until its entry gate passes (exit 2, fail-closed).
"""
import sys

print("[PHASE01-STUB] NOT_AUTHORIZED: entry gate not passed (FOUNDATION_FROZEN + WP-1 pre-consumption subgate (MST0-01 REVIEWED))")
sys.exit(2)
