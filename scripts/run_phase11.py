"""Spec PHASE 11 runner (STUB): owned by WP-4, entry gate Branch-A exact rejection.

This stub exists so every spec phase has an executable entry point.
It refuses to run until its entry gate passes (exit 2, fail-closed).
"""
import sys

print("[PHASE11-STUB] NOT_AUTHORIZED: entry gate not passed (Branch-A exact rejection)")
sys.exit(2)
