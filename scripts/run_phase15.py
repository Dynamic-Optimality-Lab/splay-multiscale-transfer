"""Spec PHASE 15 runner (STUB): owned by WP-5, entry gate TRANSFER_CALCULUS_FROZEN.

This stub exists so every spec phase has an executable entry point.
It refuses to run until its entry gate passes (exit 2, fail-closed).
"""
import sys

print("[PHASE15-STUB] NOT_AUTHORIZED: entry gate not passed (TRANSFER_CALCULUS_FROZEN)")
sys.exit(2)
