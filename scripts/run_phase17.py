"""Spec PHASE 17 runner (STUB): owned by WP-6, entry gate finite-test survival.

This stub exists so every spec phase has an executable entry point.
It refuses to run until its entry gate passes (exit 2, fail-closed).
"""
import sys

print("[PHASE17-STUB] NOT_AUTHORIZED: entry gate not passed (finite-test survival)")
sys.exit(2)
