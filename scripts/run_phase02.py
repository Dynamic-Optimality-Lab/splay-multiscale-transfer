"""Spec PHASE 02 runner (STUB): owned by WP-2, entry gate ROTATION_TRACE_CERTIFIED + MST0-02/MST0-04 REVIEWED (WP-2A first).

This stub exists so every spec phase has an executable entry point.
It refuses to run until its entry gate passes (exit 2, fail-closed).
"""
import sys

print("[PHASE02-STUB] NOT_AUTHORIZED: entry gate not passed (ROTATION_TRACE_CERTIFIED + MST0-02/MST0-04 REVIEWED (WP-2A first))")
sys.exit(2)
