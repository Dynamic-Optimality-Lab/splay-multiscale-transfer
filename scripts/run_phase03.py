"""Spec PHASE 03 runner (STUB): owned by WP-1, entry gate ROTATION_TRACE_CERTIFIED.

This stub exists so every spec phase has an executable entry point.
It refuses to run until its entry gate passes (exit 2, fail-closed).
"""
import sys

print("[PHASE03-STUB] NOT_AUTHORIZED: entry gate not passed (ROTATION_TRACE_CERTIFIED)")
sys.exit(2)
