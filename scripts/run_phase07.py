"""Spec PHASE 07 runner (STUB): owned by WP-3, entry gate WP-2 gates.

This stub exists so every spec phase has an executable entry point.
It refuses to run until its entry gate passes (exit 2, fail-closed).
"""
import sys

print("[PHASE07-STUB] NOT_AUTHORIZED: entry gate not passed (WP-2 gates)")
sys.exit(2)
